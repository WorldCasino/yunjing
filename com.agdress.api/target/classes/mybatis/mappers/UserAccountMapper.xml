<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.agdress.mapper.UserAccountMapper">

    <update id="updateBalance">
      UPDATE t_user_account SET t_user_account.balance = #{balance} WHERE t_user_account.user_id = #{userId}
    </update>

</mapper>